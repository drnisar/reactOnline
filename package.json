{
  "private": true,
  "scripts": {
    "dev": "npm --prefix react-codespaces run dev",
    "build": "npm --prefix react-codespaces run build",
    "lint": "npm --prefix react-codespaces run lint",
    "preview": "npm --prefix react-codespaces run preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "axios": "^1.13.6",
    "react-router-dom": "^7.13.2",
    "tailwindcss": "^4.2.2",
    "zustand": "^5.0.12"
  }
}
